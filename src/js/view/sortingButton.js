import View from "./View.js";

class SortingButton extends View {
  //   _sortButtons = document.querySelectorAll(".bi-caret-up-fill");
  //   sortTableByColumn(result, handler) {
  //     console.log(this._sortButtons);
  //     this._sortButtons.forEach((i) =>
  //       i.addEventListener("click", function () {
  //         result.sort((a, b) => b.id.length - a.id.length);
  //         handler();
  //       })
  //     );
  //   }
}

export default new SortingButton();
